# 웹뷰로 띄울 stream-client-web 주소.
# 이 파일을 .env.local로 복사한 뒤 값을 자기 환경에 맞게 채운다.
#
# 로컬 개발: 개발 PC의 LAN IP를 쓴다. localhost는 기기에서 기기 자신을 가리켜 닿지 않는다.
#   - macOS에서 IP 확인: ipconfig getifaddr en0
#   - 기기와 PC가 같은 네트워크에 있어야 한다
# 배포 후에는 운영 URL로 바꾼다.
EXPO_PUBLIC_WEB_URL=http://192.168.0.2:5173
